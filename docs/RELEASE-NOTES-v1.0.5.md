# Smart Replay Tracker v1.0.5

## What's new

- Added a `Mapping Source` dropdown in the Lua script UI
- Added `Add Mapping From Selected Source` so users can create or update one mapping without rebuilding the whole priority list
- Updated the script configuration guide with the new one-source mapping workflow

## Upgrade notes

- Replace `replay-file-organizer.lua` with the new file from `release/`
- `smart-replay-tracker.dll` does not require a change for this patch
